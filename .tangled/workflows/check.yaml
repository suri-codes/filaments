when:
  - event: ["push", "pull_request"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - cargo
    - clippy
    - gcc
    - just

steps:
    - name: clippy
      command: |
        cargo clippy -- -d warnings
    - name: build
      command: |
        just build

when:
  - event: ["push", "pull_request"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - cargo
    - just
    - gcc

steps:
    - name: run tests
      command: |
        just test
